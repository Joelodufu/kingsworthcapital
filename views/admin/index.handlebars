<div class="panel panel-bd lobidrag">

  <hr>
  <hr>
  <div class="table-responsive">
    <table class="table table-dark table-striped">
      <thead>

        <tr>
          <th>Name</th>
          <th>Phone</th>
          <th>Adress</th>
          <th>Email</th>
          <th>password</th>
          <th>Date Reg </th>
          <th>DOB</th>
          <th>Gender</th>
          <th>Updates</th>
        </tr>
      </thead>
      <tbody>
        {{#each allUsers}}

        <tr>


          <td>{{firstName}} {{lastName}}</td>
          <td>{{phone}}</td>
          <td>{{adress}}</td>
          <td>{{email}}</td>
          <td>{{password}}</td>
          <td> {{date}}</td>
          <td> {{datOfBirth}}</td>
          <td> {{gender}}</td>
          <td>
            <form action="/admin/delete/{{_id}}?newMethod=DELETE" method="POST">
              <button type="submit" class="btn btn-danger btn-xs"><i class="bi bi-person-x-fill"></i>Delete</button>
            </form>

          </td>
        </tr>

        {{/each}}
      </tbody>
    </table>
  </div>
  <div class="page-nation text-right">
    <ul class="pagination pagination-large">
      <li class="disabled"><span>«</span></li>
      <li class="active"><span>1</span></li>
      <li><a href="#">2</a></li>
      <li class="disabled"><span>...</span></li>
      <li>
      <li><a rel="next" href="#">Next</a></li>
    </ul>
  </div>

</div>